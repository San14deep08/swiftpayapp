target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

.idea/
*.iml
.vscode/
.settings/
.project
.classpath

.env
*.log

# Load-test PCAP captures — large binary files, regenerated per run.
# The one currently in git history (load-test/output/loadtest.pcap) is
# kept as submission evidence for the 250 TPS run; don't commit new ones.
load-test/output/.DS_Store
.DS_Store
